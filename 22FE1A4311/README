# 22FE1A4311

Repository for test submission.

## 📂 Folder Structure

```
22FE1A4311/
├── LoggingMiddleware/
│   ├── package.json
│   └── logger.js
│
├── BackendTestSubmission/
│   ├── package.json
│   └── server.js
│
└── frontend/
    ├── package.json
    ├── public/
    │   └── index.html
    └── src/
        ├── App.js
        ├── index.js
        └── StatsPage.js
```

---

## 🚀 How to Run the Backend

1. Open terminal and go to the backend folder:
   ```bash
   cd BackendTestSubmission
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the server:
   ```bash
   npm start
   ```
4. The backend will run at:
   ```
   http://localhost:5000
   ```

---

## 💻 How to Run the Frontend

1. Open a new terminal and go to the frontend folder:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the React app:
   ```bash
   npm start
   ```
4. The frontend will run at:
   ```
   http://localhost:3000
   ```

---

## 📝 Notes

- `node_modules/` and `.env` files are ignored from version control.
- Both frontend and backend must be running for the project to work properly.
- Logging is handled in the `LoggingMiddleware/` folder.

